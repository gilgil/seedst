#!/usr/bin/env python3
import sys
import os

# Add the 'src' directory to Python's path so it can find the package
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "src"))

from seedst.cli import main

if __name__ == "__main__":
    main()
